version: "3"
services:
  scylla:
    image: scylla-ubuntu:v3
    build:
      context: .
      dockerfile: Dockerfile-ubuntu
    ports:
      - "127.0.0.1:8899:8899"
      - "127.0.0.1:8081:8081"
    volumes:
      - /var/www/scylla:/var/www/scylla

